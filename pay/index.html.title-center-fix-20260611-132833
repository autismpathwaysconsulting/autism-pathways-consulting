<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Payment | Autism Pathways Consulting</title>
  <meta name="description" content="Payment page for confirmed Autism Pathways Consulting families.">
  <style>
    :root {
      --teal-dark: #073734;
      --teal: #0f766e;
      --teal-soft: #e7f6f3;
      --cream: #fffaf3;
      --card: #ffffff;
      --coral: #e8997a;
      --text: #173834;
      --muted: rgba(23, 56, 52, 0.68);
      --border: rgba(7, 55, 52, 0.13);
      --shadow: 0 10px 28px rgba(7, 55, 52, 0.07);
      --radius: 16px;
    }

    * {
      box-sizing: border-box;
    }

    html {
      scroll-behavior: smooth;
    }

    body {
      margin: 0;
      font-family: Inter, Segoe UI, Roboto, Arial, sans-serif;
      background: var(--cream);
      color: var(--text);
      line-height: 1.5;
    }

    a {
      color: inherit;
    }

    .site-header {
      position: sticky;
      top: 0;
      z-index: 20;
      background: rgba(255, 250, 243, 0.96);
      border-bottom: 1px solid var(--border);
      backdrop-filter: blur(10px);
    }

    .nav {
      width: min(1080px, calc(100% - 32px));
      min-height: 64px;
      margin: 0 auto;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 20px;
    }

    .brand {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      color: var(--teal-dark);
      text-decoration: none;
      font-weight: 900;
      letter-spacing: -0.03em;
      line-height: 1.08;
    }

    .brand img {
      width: 34px;
      height: 34px;
      object-fit: contain;
      border-radius: 8px;
    }

    .brand small {
      display: block;
      margin-top: 2px;
      color: var(--muted);
      font-size: 0.73rem;
      font-weight: 700;
      letter-spacing: 0;
    }

    .nav-links {
      display: flex;
      align-items: center;
      gap: 15px;
      color: var(--muted);
      font-size: 0.9rem;
      font-weight: 750;
      white-space: nowrap;
    }

    .nav-links a {
      text-decoration: none;
    }

    .nav-links a:hover {
      color: var(--teal-dark);
    }

    .nav-cta {
      padding: 8px 12px;
      border-radius: 999px;
      background: var(--teal-dark);
      color: var(--cream) !important;
    }

    .wrap {
      width: min(1080px, calc(100% - 32px));
      margin: 0 auto;
    }

    .hero {
      padding: 38px 0 14px;
    }

    .eyebrow {
      margin: 0 0 8px;
      color: var(--teal);
      font-size: 0.76rem;
      font-weight: 950;
      letter-spacing: 0.12em;
      text-transform: uppercase;
    }

    h1 {
      margin: 0;
      max-width: 760px;
      color: var(--teal-dark);
      font-size: clamp(2rem, 4.5vw, 3.35rem);
      line-height: 1.04;
      letter-spacing: -0.055em;
    }

    .hero-text {
      max-width: 720px;
      margin: 12px 0 0;
      color: var(--muted);
      font-size: 0.98rem;
    }

    .section {
      padding: 18px 0;
    }

    .section-title {
      margin: 0 0 12px;
      color: var(--teal-dark);
      font-size: clamp(1.3rem, 2.4vw, 1.9rem);
      line-height: 1.12;
      letter-spacing: -0.04em;
    }

    .steps-grid {
      display: grid;
      grid-template-columns: repeat(5, minmax(0, 1fr));
      gap: 9px;
    }

    .step-card {
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: 14px;
      padding: 13px;
      box-shadow: 0 8px 22px rgba(7, 55, 52, 0.04);
    }

    .step-number {
      display: inline-flex;
      width: 24px;
      height: 24px;
      align-items: center;
      justify-content: center;
      border-radius: 999px;
      background: var(--teal-dark);
      color: var(--cream);
      font-size: 0.82rem;
      font-weight: 950;
      margin-bottom: 8px;
    }

    .step-card h3 {
      margin: 0 0 4px;
      color: var(--teal-dark);
      font-size: 0.92rem;
      line-height: 1.18;
    }

    .step-card p {
      margin: 0;
      color: var(--muted);
      font-size: 0.83rem;
      line-height: 1.4;
    }

    .option-tabs {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 12px;
      margin: 12px 0 14px;
    }

    .option-tab {
      cursor: pointer;
      text-align: left;
      border: 1px solid var(--border);
      border-radius: var(--radius);
      background: var(--card);
      padding: 16px;
      box-shadow: var(--shadow);
      color: var(--text);
      transition: 0.16s ease;
    }

    .option-tab:hover {
      transform: translateY(-1px);
      border-color: rgba(15, 118, 110, 0.35);
    }

    .option-tab.active {
      border: 2px solid var(--teal);
      background: linear-gradient(180deg, #ffffff 0%, #f3fbf8 100%);
    }

    .tab-label {
      display: inline-flex;
      width: fit-content;
      margin-bottom: 7px;
      padding: 5px 9px;
      border-radius: 999px;
      background: rgba(45, 212, 191, 0.14);
      color: var(--teal);
      font-size: 0.68rem;
      font-weight: 950;
      letter-spacing: 0.08em;
      text-transform: uppercase;
    }

    .option-tab h3 {
      margin: 0 0 5px;
      color: var(--teal-dark);
      font-size: clamp(1.02rem, 2vw, 1.32rem);
      line-height: 1.12;
      letter-spacing: -0.035em;
    }

    .tab-price {
      margin: 0;
      color: var(--teal-dark);
      font-size: clamp(1.75rem, 3.5vw, 2.35rem);
      font-weight: 950;
      letter-spacing: -0.06em;
      line-height: 1;
    }

    .tab-summary {
      margin: 9px 0 0;
      color: var(--muted);
      font-size: 0.9rem;
      line-height: 1.42;
    }

    .details-panel {
      display: none;
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: clamp(17px, 2.8vw, 24px);
      box-shadow: var(--shadow);
    }

    .details-panel.active {
      display: block;
    }

    .details-top {
      display: grid;
      grid-template-columns: 1.35fr 0.65fr;
      gap: 16px;
      align-items: start;
    }

    .details-panel h2 {
      margin: 0 0 7px;
      color: var(--teal-dark);
      font-size: clamp(1.38rem, 2.8vw, 2.05rem);
      line-height: 1.08;
      letter-spacing: -0.045em;
    }

    .details-price {
      margin: 0;
      color: var(--teal-dark);
      font-size: clamp(2.15rem, 5vw, 3.45rem);
      font-weight: 950;
      letter-spacing: -0.07em;
      line-height: 1;
      text-align: right;
    }

    .details-summary {
      max-width: 680px;
      margin: 0;
      color: var(--muted);
      font-size: 0.95rem;
      line-height: 1.54;
    }

    .includes {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 8px;
      margin: 16px 0 0;
      padding: 0;
      list-style: none;
    }

    .includes li {
      position: relative;
      padding: 9px 11px 9px 32px;
      border-radius: 11px;
      background: #f7fbf8;
      border: 1px solid rgba(7, 55, 52, 0.08);
      color: var(--text);
      font-size: 0.89rem;
      font-weight: 700;
      line-height: 1.32;
    }

    .includes li::before {
      content: "✓";
      position: absolute;
      left: 12px;
      top: 9px;
      color: var(--teal);
      font-weight: 950;
    }

    .note {
      margin-top: 14px;
      padding: 12px 14px;
      border-radius: 13px;
      background: rgba(232, 153, 122, 0.12);
      border: 1px solid rgba(232, 153, 122, 0.24);
      color: #5a352a;
      font-size: 0.91rem;
      font-weight: 700;
      line-height: 1.42;
    }

    .payment-details {
      margin-top: 16px;
      padding: clamp(17px, 2.8vw, 24px);
      border-radius: var(--radius);
      background: var(--card);
      border: 1px solid var(--border);
      box-shadow: var(--shadow);
    }

    .payment-details h2 {
      margin: 0 0 7px;
      color: var(--teal-dark);
      font-size: clamp(1.3rem, 2.4vw, 1.9rem);
      line-height: 1.12;
      letter-spacing: -0.04em;
    }

    .payment-details-intro {
      margin: 0 0 14px;
      max-width: 760px;
      color: var(--muted);
      font-size: 0.93rem;
      line-height: 1.48;
    }

    .payment-methods {
      display: grid;
      grid-template-columns: 1fr 0.75fr;
      gap: 13px;
      align-items: stretch;
    }

    .payment-method-card {
      padding: 15px;
      border-radius: 15px;
      background: #f7fbf8;
      border: 1px solid rgba(7, 55, 52, 0.09);
    }

    .payment-method-card h3 {
      margin: 0 0 9px;
      color: var(--teal-dark);
      font-size: 1rem;
      line-height: 1.2;
    }

    .bank-row {
      display: grid;
      grid-template-columns: 120px 1fr;
      gap: 10px;
      padding: 7px 0;
      border-bottom: 1px solid rgba(7, 55, 52, 0.09);
    }

    .bank-row:last-child {
      border-bottom: 0;
    }

    .bank-row span {
      color: var(--muted);
      font-size: 0.82rem;
      font-weight: 850;
    }

    .bank-row strong {
      color: var(--teal-dark);
      font-weight: 900;
      word-break: break-word;
    }

    .qr-box {
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 190px;
      text-align: center;
    }

    .qr-box img {
      width: min(175px, 100%);
      height: auto;
      border-radius: 14px;
      background: #ffffff;
      border: 1px solid rgba(7, 55, 52, 0.12);
      padding: 7px;
    }

    .payment-confirmation {
      margin-top: 13px;
      padding: 12px 14px;
      border-radius: 13px;
      background: rgba(232, 153, 122, 0.12);
      border: 1px solid rgba(232, 153, 122, 0.24);
      color: #5a352a;
      font-size: 0.91rem;
      font-weight: 750;
      line-height: 1.45;
    }

    .whatsapp-confirm-button {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-height: 46px;
      margin-top: 13px;
      padding: 12px 18px;
      border-radius: 999px;
      background: var(--teal-dark);
      color: var(--cream);
      text-decoration: none;
      font-weight: 950;
      text-align: center;
    }

    .boundary {
      margin: 13px 0 0;
      color: var(--muted);
      font-size: 0.88rem;
      line-height: 1.45;
    }

    footer {
      margin-top: 24px;
      padding: 20px 0 30px;
      border-top: 1px solid var(--border);
      color: var(--muted);
      font-size: 0.88rem;
    }

    @media (max-width: 980px) {
      .option-tabs {
        grid-template-columns: 1fr;
      }

      .nav {
        align-items: flex-start;
        flex-direction: column;
        padding: 14px 0;
      }

      .nav-links {
        flex-wrap: wrap;
        gap: 12px;
      }

      .steps-grid {
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }

      .details-top,
      .payment-methods {
        grid-template-columns: 1fr;
      }

      .details-price {
        text-align: left;
      }
    }

    @media (max-width: 760px) {
      .steps-grid,
      .includes {
        grid-template-columns: 1fr;
      }

      .bank-row {
        grid-template-columns: 1fr;
        gap: 2px;
      }

      .whatsapp-confirm-button {
        width: 100%;
      }
    }
  </style>

<!-- APC_PAY_PAGE_SAFE_UPDATE_V1 -->
<style>
  .apc-pay-page {
    max-width: 1120px;
    margin: 0 auto;
    padding: 56px 20px 72px;
  }

  .apc-pay-hero {
    text-align: center;
    padding: 48px 24px 36px;
    border-radius: 28px;
    background: linear-gradient(135deg, rgba(45, 212, 191, 0.12), rgba(232, 153, 122, 0.10));
    border: 1px solid rgba(26, 26, 26, 0.08);
  }

  .apc-pay-eyebrow {
    margin: 0 0 10px;
    font-size: 0.82rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    font-weight: 700;
    color: #0f766e;
  }

  .apc-pay-hero h1 {
    margin: 0;
    font-size: clamp(2rem, 4vw, 3.25rem);
    line-height: 1.05;
    color: #1a1a1a;
  }

  .apc-pay-lead {
    max-width: 760px;
    margin: 18px auto 0;
    font-size: 1.08rem;
    line-height: 1.65;
    color: rgba(26, 26, 26, 0.78);
  }

  .apc-pay-alert {
    max-width: 760px;
    margin: 22px auto 0;
    padding: 16px 18px;
    border-radius: 18px;
    background: #fff9f1;
    border: 1px solid rgba(232, 153, 122, 0.35);
    color: rgba(26, 26, 26, 0.82);
    line-height: 1.55;
  }

  .apc-pay-actions {
    display: flex;
    justify-content: center;
    gap: 12px;
    flex-wrap: wrap;
    margin-top: 26px;
  }

  .apc-pay-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 46px;
    padding: 12px 18px;
    border-radius: 999px;
    font-weight: 700;
    text-decoration: none;
    transition: transform 0.18s ease, box-shadow 0.18s ease;
  }

  .apc-pay-button:hover {
    transform: translateY(-1px);
  }

  .apc-pay-button.primary {
    background: #1a1a1a;
    color: #ffffff;
    box-shadow: 0 10px 24px rgba(26, 26, 26, 0.16);
  }

  .apc-pay-button.secondary {
    background: #ffffff;
    color: #1a1a1a;
    border: 1px solid rgba(26, 26, 26, 0.12);
  }

  .apc-pay-section {
    margin-top: 34px;
  }

  .apc-pay-section h2 {
    margin: 0 0 16px;
    font-size: clamp(1.45rem, 2.5vw, 2rem);
    color: #1a1a1a;
  }

  .apc-pay-muted {
    color: rgba(26, 26, 26, 0.70);
    line-height: 1.65;
  }

  .apc-pay-steps {
    display: grid;
    grid-template-columns: repeat(5, minmax(0, 1fr));
    gap: 12px;
    margin-top: 18px;
  }

  .apc-pay-step {
    background: #ffffff;
    border: 1px solid rgba(26, 26, 26, 0.08);
    border-radius: 20px;
    padding: 18px 16px;
    box-shadow: 0 12px 30px rgba(26, 26, 26, 0.05);
  }

  .apc-pay-step-number {
    display: inline-flex;
    width: 32px;
    height: 32px;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    background: rgba(45, 212, 191, 0.18);
    color: #0f766e;
    font-weight: 800;
    margin-bottom: 10px;
  }

  .apc-pay-step p {
    margin: 0;
    font-size: 0.95rem;
    line-height: 1.45;
    color: rgba(26, 26, 26, 0.82);
  }

  .apc-pay-cards {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 16px;
    margin-top: 18px;
  }

  .apc-pay-card {
    background: #ffffff;
    border: 1px solid rgba(26, 26, 26, 0.08);
    border-radius: 24px;
    padding: 22px;
    box-shadow: 0 16px 36px rgba(26, 26, 26, 0.06);
  }

  .apc-pay-card h3 {
    margin: 0 0 10px;
    font-size: 1.2rem;
    color: #1a1a1a;
  }

  .apc-pay-card p {
    margin: 0 0 14px;
    color: rgba(26, 26, 26, 0.72);
    line-height: 1.62;
  }

  .apc-pay-small-note {
    margin-top: 14px;
    font-size: 0.92rem;
    color: rgba(26, 26, 26, 0.62);
    line-height: 1.55;
  }

  .apc-pay-info-box {
    background: #fff9f1;
    border: 1px solid rgba(232, 153, 122, 0.28);
    border-radius: 24px;
    padding: 24px;
  }

  .apc-pay-info-box p {
    margin: 0;
  }

  .apc-pay-after-list {
    display: grid;
    gap: 10px;
    margin: 16px 0 0;
    padding: 0;
    list-style: none;
  }

  .apc-pay-after-list li {
    padding: 14px 16px;
    border-radius: 16px;
    background: #ffffff;
    border: 1px solid rgba(26, 26, 26, 0.08);
    color: rgba(26, 26, 26, 0.80);
    line-height: 1.5;
  }

  .apc-pay-scope {
    margin-top: 26px;
    padding: 18px 20px;
    border-radius: 20px;
    background: rgba(45, 212, 191, 0.10);
    border: 1px solid rgba(45, 212, 191, 0.24);
    color: rgba(26, 26, 26, 0.78);
    line-height: 1.62;
  }

  @media (max-width: 900px) {
    .apc-pay-steps,
    .apc-pay-cards {
      grid-template-columns: 1fr;
    }

    .apc-pay-page {
      padding-top: 32px;
    }

    .apc-pay-hero {
      padding: 36px 18px 30px;
    }
  }
</style>
<!-- /APC_PAY_PAGE_SAFE_UPDATE_V1 -->




</head>
<body>
  <header class="site-header">
    <nav class="nav" aria-label="Main navigation">
      <a class="brand" href="/">
        <img src="/apc-option-d-primary-logo.png" alt="Autism Pathways Consulting logo">
        <span>
          Autism Pathways Consulting
          <small>Parent education and practical autism guidance</small>
        </span>
      </a>

      <div class="nav-links">
        <a href="/">Home</a>
        <a href="/about.html">About CJ</a>
        <a href="/services.html">Services</a>
        <a href="/resources.html">Resources</a>
        <a class="nav-cta" href="/start.html">Free 15-Min First Step Call</a>
      </div>
    </nav>
  </header>

  <main class="apc-pay-page">
  <section class="apc-pay-hero">
    <p class="apc-pay-eyebrow">Payment confirmation</p>
    <h1>Complete Your APC Payment</h1>
    <p class="apc-pay-lead">
      Please only make payment after your session or programme has been confirmed with CJ.
    </p>
    <div class="apc-pay-alert">
      After payment is completed, send your payment confirmation on WhatsApp. Once payment is confirmed, APC will issue the receipt, send the intake form where relevant, and arrange the next appointment.
    </div>
    <div class="apc-pay-actions">
      <a class="apc-pay-button primary" href="https://wa.me/601172998168?text=Hi%20CJ%2C%20I%20have%20completed%20my%20APC%20payment.%20Here%20is%20my%20payment%20confirmation.">
        Send payment confirmation on WhatsApp
      </a>
      <a class="apc-pay-button secondary" href="https://wa.me/601172998168?text=Hi%20CJ%2C%20I%20need%20help%20with%20APC%20payment.">
        Need help with payment?
      </a>
    </div>
  </section>

  <section class="apc-pay-section" aria-labelledby="payment-steps">
    <h2 id="payment-steps">Payment steps</h2>
    <div class="apc-pay-steps">
      <div class="apc-pay-step">
        <span class="apc-pay-step-number">1</span>
        <p>Confirm your session or programme with CJ.</p>
      </div>
      <div class="apc-pay-step">
        <span class="apc-pay-step-number">2</span>
        <p>Make payment using the option sent to you.</p>
      </div>
      <div class="apc-pay-step">
        <span class="apc-pay-step-number">3</span>
        <p>Send payment confirmation on WhatsApp.</p>
      </div>
      <div class="apc-pay-step">
        <span class="apc-pay-step-number">4</span>
        <p>Receive your receipt.</p>
      </div>
      <div class="apc-pay-step">
        <span class="apc-pay-step-number">5</span>
        <p>Complete the intake form and schedule Session 1, where relevant.</p>
      </div>
    </div>
  </section>

  <section class="apc-pay-section" aria-labelledby="payment-options">
    <h2 id="payment-options">Payment options</h2>
    <p class="apc-pay-muted">
      Use the payment option that CJ has confirmed with you. Please only pay the amount that has been confirmed directly.
    </p>

    <div class="apc-pay-cards">
      <article class="apc-pay-card">
        <h3>Malaysia Payment</h3>
        <p>
          For Malaysia-based families, payment is usually made by Maybank bank transfer or DuitNow QR.
        </p>
        <p class="apc-pay-small-note">
          Only pay the amount confirmed by CJ.
        </p>
        <a class="apc-pay-button primary" href="https://wa.me/601172998168?text=Hi%20CJ%2C%20I%20have%20completed%20my%20Malaysia%20payment%20for%20APC.%20Here%20is%20my%20payment%20confirmation.">
          Send payment confirmation
        </a>
      </article>

      <article class="apc-pay-card">
        <h3>Singapore Payment</h3>
        <p>
          For Singapore-based families, CJ will send a Wise SGD payment request or Wise SGD transfer details.
        </p>
        <p>
          You can usually pay from your Singapore bank app as a local SGD transfer.
        </p>
        <p class="apc-pay-small-note">
          Only use the payment details sent to you directly.
        </p>
        <a class="apc-pay-button secondary" href="https://wa.me/601172998168?text=Hi%20CJ%2C%20please%20send%20me%20the%20Singapore%20payment%20details%20for%20APC.">
          Ask CJ for Singapore payment details
        </a>
      </article>

      <article class="apc-pay-card">
        <h3>International Payment</h3>
        <p>
          For international families, CJ will send a secure card payment link or suitable international payment option.
        </p>
        <p class="apc-pay-small-note">
          Only use the payment link sent to you directly.
        </p>
        <a class="apc-pay-button secondary" href="https://wa.me/601172998168?text=Hi%20CJ%2C%20please%20send%20me%20the%20international%20payment%20details%20for%20APC.">
          Ask CJ for international payment details
        </a>
      </article>
    </div>
  </section>
<section class="apc-pay-section">
    <div class="apc-pay-info-box">
      <h2>Fees and billing region</h2>
      <p class="apc-pay-muted">
        Fees are confirmed after the First Step Call or parent support discussion, once the appropriate level of support has been identified.
      </p>
      <p class="apc-pay-muted" style="margin-top: 10px;">
        Malaysia-based families are billed in MYR. Singapore-based families are billed in SGD. Other international families are billed in USD.
      </p>
    </div>
  </section>

  <section class="apc-pay-section" aria-labelledby="after-payment">
    <h2 id="after-payment">What happens after payment?</h2>
    <ul class="apc-pay-after-list">
      <li>APC will issue your receipt.</li>
      <li>CJ will send the intake form, if required.</li>
      <li>CJ will review the intake form before Session 1.</li>
      <li>Session 1 will be scheduled after payment confirmation and intake review.</li>
    </ul>

    <div class="apc-pay-scope">
      APC provides parent education, pattern review, support planning, and practical implementation guidance. APC does not provide therapy, diagnosis, medical advice, crisis support, or guaranteed behaviour change.
    </div>
  </section>
</main>

  <footer>
    <div class="wrap">
      Autism Pathways Consulting | autismpathwaysconsulting.com | @autismpathwaysconsulting
    </div>
  </footer>

  
<!-- Old public pricing script removed by APC cleanup -->





<!-- APC_CLEAN_PAYMENT_URL_REDIRECT_V1 -->
<script>
  (function () {
    var cleanRoutes = {
      "strategy": "/pay/strategy/",
      "progress": "/pay/progress/",
      "focused": "/pay/focused/",
      "focused-1600": "/pay/focused-1600/",
      "comprehensive": "/pay/comprehensive/",
      "parent-support": "/pay/comprehensive/",
      "programme": "/pay/comprehensive/"
    };

    var params = new URLSearchParams(window.location.search);
    var key = params.get("option") || params.get("pay") || params.get("program");

    if (key && cleanRoutes[key]) {
      window.location.replace(cleanRoutes[key]);
    }
  })();
</script>
<!-- /APC_CLEAN_PAYMENT_URL_REDIRECT_V1 -->

</body>
</html>
